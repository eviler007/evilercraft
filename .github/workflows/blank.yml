name: CI

on:
  push:
    branches:
    - master

jobs:
  clone_job:
    name: clone file to ghpage
    runs-on: ubuntu-18.04
    steps:
    - name: deploy
      run: |
        cd ./dist

        
        
        
